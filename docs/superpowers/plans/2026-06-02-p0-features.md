# P0 缺口 + Notion 风格重设计 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 4 项 P0 特性（M11 备份、#2 手动加题、#19 考前模式、#21 毕业触发），同时把现有页面统一到 Notion 风格、消除多余 action。

**Architecture:** TDD 优先（纯函数先 vitest，组件后 E2E）；新功能用 TS + Rust 端 Tauri 命令；Notion 风格用 design token 注入；不可逆操作用 inline 二次确认。

**Tech Stack:** React 18 + TypeScript 5 + Tauri 2 (Rust) + Tailwind 3 + Vitest 2 + Playwright 1.6 + sql.js (browser) / tauri-plugin-sql (desktop)

**Spec:** `docs/superpowers/specs/2026-06-02-p0-features-design.md`

**Phases:**
1. Phase 1 — #21 毕业触发（最独立、最小）
2. Phase 2 — Notion 风格 token + 现有页面样式
3. Phase 3 — #2 手动加题
4. Phase 4 — #19 考前模式
5. Phase 5 — M11 备份
6. Phase 6 — 集成测试 + 全 E2E

---

## Phase 1：#21 毕业触发

### Task 1.1：TDD 写 graduation 纯函数

**Files:**
- Create: `src/lib/graduation.ts`
- Create: `src/lib/graduation.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/graduation.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { calculateMastery, shouldGraduate, recomputeQuestionMastery } from "./graduation";

describe("calculateMastery", () => {
  it("returns 0 for no answers", () => {
    expect(calculateMastery([])).toBe(0);
  });

  it("returns 100 when all answers are correct (isCorrect=1)", () => {
    const answers = [
      { is_correct: 1 }, { is_correct: 1 }, { is_correct: 1 },
    ] as any;
    expect(calculateMastery(answers)).toBe(100);
  });

  it("treats isCorrect=2 (部分对) as 0.5 weight", () => {
    const answers = [
      { is_correct: 2 }, { is_correct: 1 },
    ] as any;
    // (0.5 + 1) / 2 = 0.75 → 75
    expect(calculateMastery(answers)).toBe(75);
  });

  it("treats isCorrect=3 (待自评) as 0", () => {
    const answers = [
      { is_correct: 3 }, { is_correct: 1 },
    ] as any;
    // (0 + 1) / 2 = 0.5 → 50
    expect(calculateMastery(answers)).toBe(50);
  });

  it("treats isCorrect=0 (错) as 0", () => {
    const answers = [
      { is_correct: 0 }, { is_correct: 1 },
    ] as any;
    expect(calculateMastery(answers)).toBe(50);
  });
});

describe("shouldGraduate", () => {
  it("returns true for mastery >= 90", () => {
    expect(shouldGraduate(90)).toBe(true);
    expect(shouldGraduate(100)).toBe(true);
  });

  it("returns false for mastery < 90", () => {
    expect(shouldGraduate(89)).toBe(false);
    expect(shouldGraduate(0)).toBe(false);
  });
});

describe("recomputeQuestionMastery", () => {
  it("returns graduated status when mastery >= 90", () => {
    const answers = [
      { is_correct: 1 }, { is_correct: 1 }, { is_correct: 1 }, { is_correct: 1 },
    ] as any;
    expect(recomputeQuestionMastery(answers)).toEqual({
      newMastery: 100,
      newStatus: "graduated",
    });
  });

  it("returns active status when mastery < 90", () => {
    const answers = [
      { is_correct: 1 }, { is_correct: 0 },
    ] as any;
    expect(recomputeQuestionMastery(answers)).toEqual({
      newMastery: 50,
      newStatus: "active",
    });
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest --exclude e2e --run src/lib/graduation.test.ts`
Expected: FAIL with "Cannot find module './graduation'"

- [ ] **Step 3: 写最小实现**

`src/lib/graduation.ts`:

```ts
export type PracticeAnswerLite = {
  is_correct: 0 | 1 | 2 | 3;
};

const WEIGHTS: Record<0 | 1 | 2 | 3, number> = {
  0: 0,
  1: 1,
  2: 0.5,
  3: 0,
};

export function calculateMastery(answers: PracticeAnswerLite[]): number {
  if (answers.length === 0) return 0;
  const sum = answers.reduce((acc, a) => acc + WEIGHTS[a.is_correct], 0);
  return Math.round((sum / answers.length) * 100);
}

export function shouldGraduate(mastery: number): boolean {
  return mastery >= 90;
}

export function recomputeQuestionMastery(answers: PracticeAnswerLite[]): {
  newMastery: number;
  newStatus: "active" | "graduated";
} {
  const newMastery = calculateMastery(answers);
  return {
    newMastery,
    newStatus: shouldGraduate(newMastery) ? "graduated" : "active",
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest --exclude e2e --run src/lib/graduation.test.ts`
Expected: 7 passed

- [ ] **Step 5: Commit**

```bash
git add src/lib/graduation.ts src/lib/graduation.test.ts
git commit -m "feat(graduation): pure functions for mastery recompute + graduation threshold"
```

---

### Task 1.2：在 GradingPage 接入 graduation 触发

**Files:**
- Modify: `src/pages/student/GradingPage.tsx`

- [ ] **Step 1: 在 `handleConfirm` 末尾，INSERT 之后调用 `markIfGraduated`**

`src/pages/student/GradingPage.tsx`，在 `handleConfirm` 的 `db.execute` 之后加：

```ts
import { calculateMastery, shouldGraduate } from "../../lib/graduation";
```

把现有 `handleConfirm` 改成：

```ts
const handleConfirm = useCallback(
  async (finalResult: GradingResult) => {
    const item = items[activeIndex];
    if (!item || sessionId === null) return;

    // Persist the confirmed answer to practice_answers.
    try {
      const db = await getDb();
      await db.execute(
        `INSERT INTO practice_answers
           (session_id, generated_question_index,
            answer_image_path, ocr_result,
            is_correct, self_assessment, graded_at)
         VALUES (?, ?, ?, ?, ?, ?, datetime('now'))`,
        [
          sessionId,
          item.index,
          item.photoPath ?? null,
          item.ocrResult?.text ?? null,
          finalResult.isCorrect,
          finalResult.explanation || null,
        ]
      );

      // Recompute mastery for the source question and mark graduated if >= 90.
      // Source question id is stored in item.question when wired up by useQuestions.
      // For demo data (no question id), this is a no-op.
      const questionId = (item.question as any).id as number | undefined;
      if (questionId) {
        const answers = (await db.select<{ is_correct: 0 | 1 | 2 | 3 }[]>(
          `SELECT is_correct FROM practice_answers
           WHERE question_id = ? OR (session_id = ? AND generated_question_index = ?)`,
          [questionId, sessionId, item.index]
        )) as { is_correct: 0 | 1 | 2 | 3 }[];
        const newMastery = calculateMastery(answers);
        const newStatus = shouldGraduate(newMastery) ? "graduated" : "active";
        await db.execute(
          `UPDATE questions SET mastery_score = ?, status = ? WHERE id = ?`,
          [newMastery, newStatus, questionId]
        );
      }
    } catch (err) {
      console.error("Failed to save practice answer:", err);
    }

    setItems((prev) =>
      prev.map((it, i) =>
        i === activeIndex
          ? { ...it, finalResult, status: "confirmed" }
          : it
      )
    );
  },
  [activeIndex, items, sessionId]
);
```

- [ ] **Step 2: 跑 build 确认类型通过**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无 GradingPage 相关错误

- [ ] **Step 3: Commit**

```bash
git add src/pages/student/GradingPage.tsx
git commit -m "feat(graduation): trigger mastery recompute and status update after confirm"
```

---

### Task 1.3：QuestionsPage 错题列表加"已毕业"chip

**Files:**
- Modify: `src/pages/student/QuestionsPage.tsx`

- [ ] **Step 1: 在错题列表渲染处添加 chip**

找到错题列表渲染 `<div key={...}>` 区域，添加 status 判断。在 `item.status === "graduated"` 时显示 chip：

```tsx
<span
  className={`text-xs px-1.5 py-0.5 rounded ${
    item.status === "graduated"
      ? "bg-green-50 text-green-700"
      : "bg-gray-100 text-gray-600"
  }`}
>
  {item.status === "graduated" ? "已毕业" : item.status === "archived" ? "已归档" : "活跃"}
</span>
```

- [ ] **Step 2: 跑 build 确认通过**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无错误

- [ ] **Step 3: Commit**

```bash
git add src/pages/student/QuestionsPage.tsx
git commit -m "feat(graduation): show graduated chip on question list"
```

---

### Task 1.4：写 E2E 测试（04-graduation）

**Files:**
- Create: `e2e/04-graduation.spec.ts`

- [ ] **Step 1: 写 E2E**

`e2e/04-graduation.spec.ts`:

```ts
import { test, expect } from "./fixtures";

test.describe("#21 毕业触发", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
    await page.evaluate(async () => {
      try { await (window as any).__TEST_CLEAR_DATA__(); } catch {}
    });
  });

  test("批改一条题到 mastery >= 90 后，错题本显示已毕业", async ({ page }) => {
    // setup
    await page.click("text=添加学生");
    await page.fill('input[type="text"]', "毕业测试生");
    await page.selectOption("select >> nth=0", "8");
    await page.selectOption("select >> nth=1", "2");
    await page.click('button:has-text("添加")');
    await page.click("text=毕业测试生");

    // seed a question with id
    const questionId = await page.evaluate(async () => {
      const mod = await import("/src/lib/db.ts");
      const db = await mod.getDb();
      const s = await db.select("SELECT id FROM students WHERE name = ?", ["毕业测试生"]);
      const sid = s[0].id;
      const r = await db.execute(
        `INSERT INTO questions (student_id, subject, source_type, question_type, content,
          correct_answer, error_cause, difficulty, mastery_score, chapter, status)
         VALUES (?, 'math', 'manual', 'objective', '题', 'A', 'concept', 'easy', 0, 'x', 'active')`,
        [sid]
      );
      return r.lastInsertId;
    });

    // seed practice_sessions and 4 correct answers
    await page.evaluate(async (qid) => {
      const mod = await import("/src/lib/db.ts");
      const db = await mod.getDb();
      const s = await db.select("SELECT id FROM students WHERE name = ?", ["毕业测试生"]);
      const sid = s[0].id;
      const sessR = await db.execute(
        "INSERT INTO practice_sessions (student_id, session_type) VALUES (?, 'ad_hoc')",
        [sid]
      );
      const sessId = sessR.lastInsertId;
      for (let i = 0; i < 4; i++) {
        await db.execute(
          `INSERT INTO practice_answers
             (session_id, question_id, generated_question_index, is_correct, graded_at)
           VALUES (?, ?, 0, 1, datetime('now'))`,
          [sessId, qid]
        );
      }
    }, questionId);

    // trigger graduation via app code path
    await page.evaluate(async (qid) => {
      const mod = await import("/src/lib/graduation.ts");
      const dbMod = await import("/src/lib/db.ts");
      const db = await dbMod.getDb();
      const ans = await db.select(
        "SELECT is_correct FROM practice_answers WHERE question_id = ?",
        [qid]
      );
      const { recomputeQuestionMastery } = mod;
      const { newMastery, newStatus } = recomputeQuestionMastery(
        ans.map((a: any) => ({ is_correct: a.is_correct }))
      );
      await db.execute(
        "UPDATE questions SET mastery_score = ?, status = ? WHERE id = ?",
        [newMastery, newStatus, qid]
      );
    }, questionId);

    // verify in UI
    await page.click("text=错题本");
    await expect(page.locator("text=已毕业").first()).toBeVisible({ timeout: 5000 });

    // verify in db
    const status = await page.evaluate(async (qid) => {
      const mod = await import("/src/lib/db.ts");
      const db = await mod.getDb();
      const r = await db.select("SELECT status, mastery_score FROM questions WHERE id = ?", [qid]);
      return r[0];
    }, questionId);
    expect(status.status).toBe("graduated");
    expect(status.mastery_score).toBe(100);
  });
});
```

- [ ] **Step 2: 跑 E2E**

Run: `npx playwright test e2e/04-graduation.spec.ts --reporter=list`
Expected: 1 passed

- [ ] **Step 3: Commit**

```bash
git add e2e/04-graduation.spec.ts
git commit -m "test(e2e): #21 graduation end-to-end"
```

---

## Phase 2：Notion 风格 token + 现有页面样式

### Task 2.1：注入 Notion 风格 token

**Files:**
- Modify: `src/index.css`
- Modify: `tailwind.config.js`
- Create: `src/styles/tokens.ts`

- [ ] **Step 1: 创建 tokens.ts**

`src/styles/tokens.ts`:

```ts
export const tokens = {
  color: {
    bg: "#ffffff",
    surface: "#fbfbfa",
    border: "#e9e9e7",
    textPrimary: "#37352f",
    textSecondary: "#787774",
    textTertiary: "#b4b4b0",
    accent: "#2383e2",
    accentBg: "#ddebf1",
    danger: "#e03e3e",
    success: "#0f7b6c",
    warning: "#cb912f",
  },
  radius: { sm: "4px", md: "6px", lg: "8px" },
  font: {
    stack: '-apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif',
  },
  shadow: {
    hover: "0 1px 2px rgba(15,15,15,0.05)",
    modal: "0 8px 24px rgba(15,15,15,0.12)",
  },
} as const;
```

- [ ] **Step 2: 修改 tailwind.config.js 扩展 theme**

`tailwind.config.js` 完整替换为：

```js
/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./index.html", "./src/**/*.{ts,tsx}"],
  theme: {
    extend: {
      colors: {
        notion: {
          bg: "#ffffff",
          surface: "#fbfbfa",
          border: "#e9e9e7",
          text: "#37352f",
          muted: "#787774",
          subtle: "#b4b4b0",
          accent: "#2383e2",
          "accent-bg": "#ddebf1",
          danger: "#e03e3e",
          success: "#0f7b6c",
          warning: "#cb912f",
        },
      },
      borderRadius: {
        notion: "6px",
      },
      fontFamily: {
        notion: [
          "-apple-system",
          "BlinkMacSystemFont",
          "Segoe UI",
          "PingFang SC",
          "Microsoft YaHei",
          "sans-serif",
        ],
      },
      boxShadow: {
        "notion-hover": "0 1px 2px rgba(15,15,15,0.05)",
        "notion-modal": "0 8px 24px rgba(15,15,15,0.12)",
      },
    },
  },
  plugins: [],
};
```

- [ ] **Step 3: 修改 src/index.css 注入基础样式**

`src/index.css` 完整替换为：

```css
@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html, body, #root {
    height: 100%;
    margin: 0;
  }
  body {
    font-family: -apple-system, BlinkMacSystemFont, "Segoe UI", "PingFang SC", "Microsoft YaHei", sans-serif;
    color: #37352f;
    background: #ffffff;
    -webkit-font-smoothing: antialiased;
  }
  button { font-family: inherit; }
}

@layer components {
  .notion-card {
    @apply bg-white border border-notion-border rounded-notion p-4;
  }
  .notion-btn {
    @apply inline-flex items-center gap-1.5 px-3 py-1.5 text-sm font-medium rounded-notion transition-colors duration-150;
  }
  .notion-btn-primary {
    @apply notion-btn bg-notion-accent text-white hover:bg-blue-600;
  }
  .notion-btn-ghost {
    @apply notion-btn text-notion-text hover:bg-notion-surface;
  }
  .notion-btn-danger {
    @apply notion-btn text-notion-danger hover:bg-red-50;
  }
  .notion-input {
    @apply w-full px-3 py-1.5 text-sm bg-white border border-notion-border rounded-notion focus:outline-none focus:border-notion-accent;
  }
  .notion-chip {
    @apply inline-flex items-center px-1.5 py-0.5 text-xs rounded;
  }
}
```

- [ ] **Step 4: 跑 build 确认通过**

Run: `npm run build`
Expected: 通过

- [ ] **Step 5: Commit**

```bash
git add src/styles/tokens.ts tailwind.config.js src/index.css
git commit -m "style(tokens): add Notion-style design tokens"
```

---

### Task 2.2：Notion 化 AppShell + Nav

**Files:**
- Modify: `src/components/layout/AppShell.tsx`
- Modify: `src/components/layout/StudentNav.tsx`
- Modify: `src/components/layout/ParentNav.tsx`
- Modify: `src/components/layout/RoleToggle.tsx`
- Modify: `src/components/layout/OllamaStatusBar.tsx`
- Modify: `src/components/layout/StudentSwitcher.tsx`

- [ ] **Step 1: AppShell 改 Notion 风**

`src/components/layout/AppShell.tsx` 替换为：

```tsx
import { ReactNode } from "react";
import { useApp } from "../../context/AppContext";
import StudentSwitcher from "./StudentSwitcher";
import StudentNav from "./StudentNav";
import ParentNav from "./ParentNav";
import RoleToggle from "./RoleToggle";
import OllamaStatusBar from "./OllamaStatusBar";

interface Props { children: ReactNode; }

function gradeLabel(grade?: number): string {
  if (!grade) return "";
  return ["", "", "初二", "初三", "高一", "高二", "高三"][grade] ?? "";
}

export default function AppShell({ children }: Props) {
  const { roleMode, currentStudent } = useApp();
  return (
    <div className="h-screen flex bg-white text-notion-text font-notion">
      <aside className="w-60 border-r border-notion-border flex flex-col">
        <div className="px-4 py-3 border-b border-notion-border">
          <span className="text-sm font-semibold">
            {roleMode === "student" ? "学习中心" : "家长中心"}
          </span>
        </div>
        <StudentSwitcher />
        <div className="flex-1 py-2 overflow-auto">
          {roleMode === "student" ? <StudentNav /> : <ParentNav />}
        </div>
        <div className="p-3 border-t border-notion-border">
          <RoleToggle />
        </div>
      </aside>
      <div className="flex-1 flex flex-col overflow-hidden">
        <header className="h-12 px-6 flex items-center justify-between border-b border-notion-border">
          <h1 className="text-sm font-medium text-notion-text">
            {currentStudent ? `${currentStudent.name} · ${gradeLabel(currentStudent.current_grade)}` : "请选择一个学生"}
          </h1>
          <OllamaStatusBar />
        </header>
        <main className="flex-1 overflow-auto px-6 py-5">{children}</main>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: StudentNav 改 Notion 风**

`src/components/layout/StudentNav.tsx` 完整替换为：

```tsx
import { Home, BookOpen, GitBranch, FileText, Camera, BarChart3 } from "lucide-react";
import { useApp } from "../../context/AppContext";

const items = [
  { id: "home", label: "首页", icon: Home },
  { id: "questions", label: "错题本", icon: BookOpen },
  { id: "graph", label: "知识图谱", icon: GitBranch },
  { id: "practice", label: "练习卷", icon: FileText },
  { id: "grading", label: "批改", icon: Camera },
  { id: "stats", label: "统计", icon: BarChart3 },
];

export default function StudentNav() {
  const { activePage, setActivePage } = useApp();
  return (
    <nav className="flex flex-col gap-0.5 px-2">
      {items.map((it) => {
        const active = activePage === it.id;
        return (
          <button
            key={it.id}
            onClick={() => setActivePage(it.id)}
            className={`flex items-center gap-2.5 px-2.5 py-1.5 text-sm rounded-notion transition-colors duration-150 ${
              active ? "bg-notion-accent-bg text-notion-text font-medium" : "text-notion-muted hover:bg-notion-surface"
            }`}
          >
            <it.icon size={16} />
            <span>{it.label}</span>
          </button>
        );
      })}
    </nav>
  );
}
```

- [ ] **Step 3: ParentNav 改 Notion 风**

`src/components/layout/ParentNav.tsx` 完整替换为：

```tsx
import { BarChart3, Settings } from "lucide-react";
import { useApp } from "../../context/AppContext";

const items = [
  { id: "dashboard", label: "学习概况", icon: BarChart3 },
  { id: "settings", label: "设置", icon: Settings },
];

export default function ParentNav() {
  const { activePage, setActivePage } = useApp();
  return (
    <nav className="flex flex-col gap-0.5 px-2">
      {items.map((it) => {
        const active = activePage === it.id;
        return (
          <button
            key={it.id}
            onClick={() => setActivePage(it.id)}
            className={`flex items-center gap-2.5 px-2.5 py-1.5 text-sm rounded-notion transition-colors duration-150 ${
              active ? "bg-notion-accent-bg text-notion-text font-medium" : "text-notion-muted hover:bg-notion-surface"
            }`}
          >
            <it.icon size={16} />
            <span>{it.label}</span>
          </button>
        );
      })}
    </nav>
  );
}
```

- [ ] **Step 4: RoleToggle 改 Notion 风**

`src/components/layout/RoleToggle.tsx` 完整替换为：

```tsx
import { useApp } from "../../context/AppContext";

export default function RoleToggle() {
  const { roleMode, toggleRoleMode } = useApp();
  const isStudent = roleMode === "student";
  return (
    <button
      onClick={toggleRoleMode}
      className="w-full flex items-center text-xs text-notion-muted hover:text-notion-text border border-notion-border rounded-notion px-2 py-1 transition-colors"
    >
      <span className="flex-1 text-center py-0.5">
        {isStudent ? "学生模式" : "家长模式"}
      </span>
      <span className="text-notion-subtle">↔</span>
    </button>
  );
}
```

- [ ] **Step 5: OllamaStatusBar 改紧凑**

`src/components/layout/OllamaStatusBar.tsx` 完整替换为：

```tsx
import { useEffect, useState } from "react";
import { checkOllamaStatus, type OllamaStatus } from "../../lib/ollamaStatus";

export default function OllamaStatusBar() {
  const [status, setStatus] = useState<OllamaStatus | null>(null);
  useEffect(() => {
    checkOllamaStatus().then(setStatus).catch(() => setStatus(null));
  }, []);
  if (!status) return <span className="text-xs text-notion-subtle">检测中…</span>;
  const dot = status.available ? "bg-green-500" : "bg-red-500";
  return (
    <span className="inline-flex items-center gap-1.5 text-xs text-notion-muted">
      <span className={`w-1.5 h-1.5 rounded-full ${dot}`} />
      <span>{status.modelName ?? "未配置"}</span>
    </span>
  );
}
```

- [ ] **Step 6: StudentSwitcher 改 Notion 风**

读 `src/components/layout/StudentSwitcher.tsx` 当前实现，将 `bg-primary-50` `bg-white` `border-gray-200` 全部替换为 `bg-notion-accent-bg` / `bg-white` / `border-notion-border`，移除 `rounded-xl` `shadow-sm` 等装饰。

具体样式替换规则：
- `rounded-xl` → `rounded-notion`
- `border-gray-200` → `border-notion-border`
- `border-gray-100` → `border-notion-border`
- `bg-primary-50` → `bg-notion-accent-bg`
- `text-primary-700` → `text-notion-text`
- `text-gray-800` → `text-notion-text`
- `text-gray-600` → `text-notion-muted`
- `text-gray-400` → `text-notion-subtle`
- `text-gray-500` → `text-notion-muted`
- `hover:bg-gray-50` → `hover:bg-notion-surface`
- `shadow-sm` `shadow-md` `shadow-lg` → 删除或 `shadow-notion-hover`
- `bg-gradient-*` → 删除

- [ ] **Step 7: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 8: Commit**

```bash
git add src/components/layout/ src/index.css tailwind.config.js
git commit -m "style(layout): Notion-fy AppShell + Nav + Switcher"
```

---

### Task 2.3：Notion 化 HomePage

**Files:**
- Modify: `src/pages/student/HomePage.tsx`

- [ ] **Step 1: 替换 hero + 卡片样式**

将 HomePage 中：
- `card bg-gradient-to-r from-primary-50 to-primary-100 border-primary-200` → `notion-card`
- `bg-primary-50` → `bg-notion-accent-bg`
- `text-primary-800` → `text-notion-text`
- `text-primary-600` → `text-notion-muted`
- `text-primary-700` → `text-notion-text`
- `btn-primary` → `notion-btn-primary`
- `card` → `notion-card`
- `text-primary-200` → `text-notion-subtle`
- `text-gray-500` → `text-notion-muted`
- `text-gray-400` → `text-notion-subtle`
- `text-gray-800` → `text-notion-text`
- `text-gray-600` → `text-notion-muted`
- `bg-gray-100` → `bg-notion-surface`
- `hover:shadow-md transition-shadow` → `hover:shadow-notion-hover transition-shadow`
- `bg-primary-200` → `text-notion-subtle`
- `rounded-xl` → `rounded-notion`

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/pages/student/HomePage.tsx
git commit -m "style(home): Notion-fy HomePage"
```

---

### Task 2.4：Notion 化 QuestionsPage / GraphPage / PracticePage / GradingPage / StatsPage / DashboardPage

**Files:**
- Modify: `src/pages/student/QuestionsPage.tsx`
- Modify: `src/pages/student/GraphPage.tsx`
- Modify: `src/pages/student/PracticePage.tsx`
- Modify: `src/pages/student/GradingPage.tsx`
- Modify: `src/pages/student/StatsPage.tsx`
- Modify: `src/pages/parent/DashboardPage.tsx`

- [ ] **Step 1: 通用替换规则（每个文件都用）**

- `rounded-xl` → `rounded-notion`
- `rounded-lg` → `rounded-notion`
- `rounded-md` → `rounded-notion`
- `border-gray-200` → `border-notion-border`
- `border-gray-100` → `border-notion-border`
- `border-gray-300` → `border-notion-border`
- `bg-white` 保持不变
- `bg-gray-50` → `bg-notion-surface`
- `bg-gray-100` → `bg-notion-surface`
- `text-gray-800` → `text-notion-text`
- `text-gray-700` → `text-notion-text`
- `text-gray-600` → `text-notion-muted`
- `text-gray-500` → `text-notion-muted`
- `text-gray-400` → `text-notion-subtle`
- `hover:bg-gray-50` → `hover:bg-notion-surface`
- `hover:bg-gray-100` → `hover:bg-notion-surface`
- `bg-primary-50` → `bg-notion-accent-bg`
- `text-primary-700` → `text-notion-text`
- `text-primary-600` → `text-notion-muted`
- `text-primary-800` → `text-notion-text`
- `bg-primary-200` → 删除或 `bg-notion-subtle/20`
- `shadow-sm` `shadow-md` `shadow-lg` → 删除或 `shadow-notion-hover`
- `bg-gradient-*` → 删除
- `card` class → `notion-card`
- `btn-primary` → `notion-btn-primary`
- `btn-secondary` → `notion-btn-ghost`
- `input` 或 `border border-gray-300 rounded-md` → `notion-input`

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过（不引入新错误）

- [ ] **Step 3: 跑现有单元测试确认未受影响**

Run: `npx vitest --exclude e2e --run`
Expected: 95 passed

- [ ] **Step 4: Commit**

```bash
git add src/pages/
git commit -m "style(pages): Notion-fy remaining 6 pages"
```

---

### Task 2.5：风格 smoke E2E

**Files:**
- Create: `e2e/08-style.spec.ts`

- [ ] **Step 1: 写 smoke E2E**

`e2e/08-style.spec.ts`:

```ts
import { test, expect } from "./fixtures";

test.describe("Notion 风格", () => {
  test("按钮使用 notion-btn-primary 类", async ({ page }) => {
    await page.goto("/");
    const buttons = page.locator("button");
    const count = await buttons.count();
    expect(count).toBeGreaterThan(0);
  });

  test("侧边栏用白底 + 细边线", async ({ page }) => {
    await page.goto("/");
    const aside = page.locator("aside").first();
    await expect(aside).toBeVisible();
    const borderColor = await aside.evaluate((el) =>
      getComputedStyle(el).borderRightColor
    );
    // notion-border = #e9e9e7
    expect(borderColor).toBe("rgb(233, 233, 231)");
  });

  test("body 使用系统字体栈", async ({ page }) => {
    await page.goto("/");
    const family = await page.evaluate(() => getComputedStyle(document.body).fontFamily);
    expect(family.toLowerCase()).toContain("apple-system");
  });
});
```

- [ ] **Step 2: 跑 E2E**

Run: `npx playwright test e2e/08-style.spec.ts --reporter=list`
Expected: 3 passed

- [ ] **Step 3: Commit**

```bash
git add e2e/08-style.spec.ts
git commit -m "test(e2e): Notion style smoke"
```

---

## Phase 3：#2 手动加题

### Task 3.1：TDD 写 manualQuestion 纯函数

**Files:**
- Create: `src/lib/manualQuestion.ts`
- Create: `src/lib/manualQuestion.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/manualQuestion.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { buildManualQuestionInput, validateManualQuestionForm } from "./manualQuestion";

describe("validateManualQuestionForm", () => {
  it("returns error when content is empty", () => {
    const errors = validateManualQuestionForm({ content: "" } as any);
    expect(errors).toContain("题目内容不能为空");
  });

  it("returns no errors when content is present", () => {
    const errors = validateManualQuestionForm({ content: "求 x" } as any);
    expect(errors).toEqual([]);
  });
});

describe("buildManualQuestionInput", () => {
  it("builds a QuestionInput with defaults", () => {
    const input = buildManualQuestionInput(
      {
        content: "求 x",
        questionType: "objective",
        subject: "math",
        chapter: "一元一次方程",
        correctAnswer: "x=4",
        errorCause: "concept",
        difficulty: "easy",
      },
      42,
      []
    );
    expect(input).toMatchObject({
      student_id: 42,
      subject: "math",
      source_type: "manual",
      question_type: "objective",
      content: "求 x",
      chapter: "一元一次方程",
      correct_answer: "x=4",
      error_cause: "concept",
      difficulty: "easy",
      mastery_score: 0,
      status: "active",
    });
  });

  it("serializes image paths to JSON array", () => {
    const input = buildManualQuestionInput(
      { content: "q", questionType: "objective", subject: "math", chapter: "c", correctAnswer: "a", errorCause: "unknown", difficulty: "medium" } as any,
      1,
      ["/a/b.png", "/a/c.png"]
    );
    expect(input.content_images).toBe('["/a/b.png","/a/c.png"]');
  });

  it("writes empty JSON array when no images", () => {
    const input = buildManualQuestionInput(
      { content: "q", questionType: "objective", subject: "math", chapter: "c", correctAnswer: "a", errorCause: "unknown", difficulty: "medium" } as any,
      1,
      []
    );
    expect(input.content_images).toBe("[]");
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest --exclude e2e --run src/lib/manualQuestion.test.ts`
Expected: FAIL "Cannot find module"

- [ ] **Step 3: 写最小实现**

`src/lib/manualQuestion.ts`:

```ts
import type { Question } from "../types";

export type ManualQuestionForm = {
  content: string;
  questionType: "objective" | "subjective";
  subject: "math" | "physics";
  chapter: string;
  correctAnswer: string;
  errorCause: Question["error_cause"];
  difficulty: Question["difficulty"];
};

export type QuestionInput = Omit<Question, "id" | "created_at" | "updated_at">;

export function validateManualQuestionForm(form: ManualQuestionForm): string[] {
  const errors: string[] = [];
  if (!form.content || !form.content.trim()) {
    errors.push("题目内容不能为空");
  }
  return errors;
}

export function buildManualQuestionInput(
  form: ManualQuestionForm,
  currentStudentId: number,
  imagePaths: string[]
): QuestionInput {
  return {
    student_id: currentStudentId,
    subject: form.subject,
    source_type: "manual",
    source_file: null,
    number_in_source: null,
    question_type: form.questionType,
    chapter: form.chapter || null,
    answer_date: null,
    content: form.content,
    content_html: null,
    content_images: JSON.stringify(imagePaths),
    student_answer: null,
    correct_answer: form.correctAnswer || null,
    error_cause: form.errorCause || "unknown",
    difficulty: form.difficulty || "medium",
    mastery_score: 0,
    status: "active",
  };
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest --exclude e2e --run src/lib/manualQuestion.test.ts`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add src/lib/manualQuestion.ts src/lib/manualQuestion.test.ts
git commit -m "feat(manual-question): pure functions for manual question input"
```

---

### Task 3.2：写 ManualAddQuestionForm 组件

**Files:**
- Create: `src/components/question/ManualAddQuestionForm.tsx`

- [ ] **Step 1: 写组件**

`src/components/question/ManualAddQuestionForm.tsx`:

```tsx
import { useState, useRef } from "react";
import { X, Upload, Loader2 } from "lucide-react";
import { invoke } from "@tauri-apps/api/core";
import {
  validateManualQuestionForm,
  buildManualQuestionInput,
  type ManualQuestionForm,
} from "../../lib/manualQuestion";
import { getDb } from "../../lib/db";
import { useQuestionAnalysis } from "../../hooks/useQuestionAnalysis";
import { useQuestions } from "../../hooks/useQuestions";

interface Props {
  studentId: number;
  onClose: () => void;
  onAdded: () => void;
}

export default function ManualAddQuestionForm({ studentId, onClose, onAdded }: Props) {
  const [form, setForm] = useState<ManualQuestionForm>({
    content: "",
    questionType: "objective",
    subject: "math",
    chapter: "",
    correctAnswer: "",
    errorCause: "unknown",
    difficulty: "medium",
  });
  const [imagePaths, setImagePaths] = useState<string[]>([]);
  const [submitting, setSubmitting] = useState(false);
  const [error, setError] = useState<string | null>(null);
  const fileInputRef = useRef<HTMLInputElement>(null);
  const { addQuestions } = useQuestions(studentId);
  const { analyzeSingle } = useQuestionAnalysis();

  function update<K extends keyof ManualQuestionForm>(key: K, val: ManualQuestionForm[K]) {
    setForm((f) => ({ ...f, [key]: val }));
  }

  async function handleUploadImages(e: React.ChangeEvent<HTMLInputElement>) {
    const files = Array.from(e.target.files ?? []);
    if (files.length === 0) return;
    const paths: string[] = [];
    for (const f of files) {
      const buf = await f.arrayBuffer();
      const path = (await invoke("save_uploaded_photo", {
        studentId,
        filename: f.name,
        bytes: Array.from(new Uint8Array(buf)),
      })) as string;
      paths.push(path);
    }
    setImagePaths((p) => [...p, ...paths]);
  }

  async function handleSubmit() {
    setError(null);
    const errs = validateManualQuestionForm(form);
    if (errs.length) {
      setError(errs.join("；"));
      return;
    }
    setSubmitting(true);
    try {
      const input = buildManualQuestionInput(form, studentId, imagePaths);
      const added = await addQuestions([input]);
      // try to analyze (non-blocking if fails)
      if (added && added[0]) {
        analyzeSingle(added[0].id).catch(() => {});
      }
      onAdded();
      onClose();
    } catch (e) {
      setError(e instanceof Error ? e.message : "添加失败");
    } finally {
      setSubmitting(false);
    }
  }

  return (
    <div className="border border-notion-border rounded-notion bg-white p-4 space-y-3">
      <div className="flex items-center justify-between">
        <h3 className="text-sm font-semibold">手动添加错题</h3>
        <button onClick={onClose} className="notion-btn-ghost p-1">
          <X size={16} />
        </button>
      </div>
      <p className="text-xs text-notion-muted">关闭即丢失，未自动暂存。</p>

      <textarea
        className="notion-input min-h-[100px]"
        placeholder="题目内容 *"
        value={form.content}
        onChange={(e) => update("content", e.target.value)}
        autoFocus
      />

      <div className="flex gap-3 flex-wrap">
        <fieldset className="flex items-center gap-2 text-sm">
          <legend className="sr-only">题型</legend>
          <span className="text-notion-muted">题型</span>
          {(["objective", "subjective"] as const).map((t) => (
            <label key={t} className="flex items-center gap-1">
              <input
                type="radio"
                name="qtype"
                checked={form.questionType === t}
                onChange={() => update("questionType", t)}
              />
              {t === "objective" ? "客观" : "主观"}
            </label>
          ))}
        </fieldset>
        <fieldset className="flex items-center gap-2 text-sm">
          <legend className="sr-only">学科</legend>
          <span className="text-notion-muted">学科</span>
          {(["math", "physics"] as const).map((s) => (
            <label key={s} className="flex items-center gap-1">
              <input
                type="radio"
                name="subject"
                checked={form.subject === s}
                onChange={() => update("subject", s)}
              />
              {s === "math" ? "数学" : "物理"}
            </label>
          ))}
        </fieldset>
      </div>

      <div className="grid grid-cols-2 gap-3">
        <input
          className="notion-input"
          placeholder="章节"
          value={form.chapter}
          onChange={(e) => update("chapter", e.target.value)}
          list="chapter-suggestions"
        />
        <datalist id="chapter-suggestions">
          {/* Could be populated from existing chapters later */}
        </datalist>
        <input
          className="notion-input"
          placeholder="参考答案"
          value={form.correctAnswer}
          onChange={(e) => update("correctAnswer", e.target.value)}
        />
      </div>

      <div className="flex gap-3">
        <select
          className="notion-input flex-1"
          value={form.errorCause}
          onChange={(e) => update("errorCause", e.target.value as any)}
        >
          <option value="unknown">错因：未分类</option>
          <option value="concept">概念不清</option>
          <option value="calculation">计算错误</option>
          <option value="careless">粗心</option>
          <option value="misread">审题失误</option>
        </select>
        <select
          className="notion-input flex-1"
          value={form.difficulty}
          onChange={(e) => update("difficulty", e.target.value as any)}
        >
          <option value="easy">难度：易</option>
          <option value="medium">难度：中</option>
          <option value="hard">难度：难</option>
        </select>
      </div>

      <div>
        <input
          ref={fileInputRef}
          type="file"
          accept="image/*"
          multiple
          className="hidden"
          onChange={handleUploadImages}
        />
        <button
          onClick={() => fileInputRef.current?.click()}
          className="notion-btn-ghost text-xs"
          type="button"
        >
          <Upload size={14} /> 上传图片（{imagePaths.length}）
        </button>
      </div>

      {error && <p className="text-sm text-notion-danger">{error}</p>}

      <div className="flex justify-end gap-2 pt-2">
        <button onClick={onClose} className="notion-btn-ghost">取消</button>
        <button onClick={handleSubmit} disabled={submitting} className="notion-btn-primary">
          {submitting && <Loader2 size={14} className="animate-spin" />}
          完成添加
        </button>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 跑 build 验证类型**

Run: `npx tsc --noEmit -p tsconfig.json`
Expected: 无 ManualAddQuestionForm 相关错误

- [ ] **Step 3: Commit**

```bash
git add src/components/question/ManualAddQuestionForm.tsx
git commit -m "feat(manual-question): inline form panel for adding questions"
```

---

### Task 3.3：在 QuestionsPage 接入 form 入口

**Files:**
- Modify: `src/pages/student/QuestionsPage.tsx`

- [ ] **Step 1: 添加 state + form 渲染**

在 `QuestionsPage` 顶部加：

```ts
import { useState } from "react";
import { Plus } from "lucide-react";
import ManualAddQuestionForm from "../../components/question/ManualAddQuestionForm";
```

在 `QuestionsPage` 函数内加 state：

```ts
const [adding, setAdding] = useState(false);
```

把"导入 Word"按钮所在的顶栏，加一个"添加"按钮：

```tsx
<button
  onClick={() => setAdding((s) => !s)}
  className="notion-btn-ghost"
>
  <Plus size={16} />
  {adding ? "收起" : "添加"}
</button>
```

在错题列表之前，条件渲染 form：

```tsx
{adding && currentStudent && (
  <ManualAddQuestionForm
    studentId={currentStudent.id}
    onClose={() => setAdding(false)}
    onAdded={() => refresh()}
  />
)}
```

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/pages/student/QuestionsPage.tsx
git commit -m "feat(manual-question): wire add button into QuestionsPage"
```

---

### Task 3.4：添加 save_uploaded_photo Tauri 命令

**Files:**
- Modify: `src-tauri/src/commands/grading.rs`
- Modify: `src-tauri/src/commands/mod.rs`

- [ ] **Step 1: 添加 Rust 命令**

`src-tauri/src/commands/grading.rs` 末尾添加：

```rust
use std::fs;
use std::path::PathBuf;
use tauri::AppHandle;
use tauri::Manager;

#[tauri::command]
pub fn save_uploaded_photo(
    app: AppHandle,
    student_id: i64,
    filename: String,
    bytes: Vec<u8>,
) -> Result<String, String> {
    let dir: PathBuf = app
        .path()
        .app_data_dir()
        .map_err(|e| e.to_string())?
        .join("photos")
        .join(student_id.to_string());
    fs::create_dir_all(&dir).map_err(|e| e.to_string())?;
    let ts = std::time::SystemTime::now()
        .duration_since(std::time::UNIX_EPOCH)
        .unwrap()
        .as_millis();
    let safe_name = filename.replace(['/', '\\', ':', ' '], "_");
    let path = dir.join(format!("{}-{}", ts, safe_name));
    fs::write(&path, &bytes).map_err(|e| e.to_string())?;
    Ok(path.to_string_lossy().to_string())
}
```

`src-tauri/src/commands/mod.rs` 暴露 `save_uploaded_photo`（在 `pub mod grading;` 之后追加 `pub use`）：

```rust
pub mod backup;
pub mod grading;

pub use backup::{
    backup_to_sync_folder, cleanup_old_snapshots, create_local_snapshot,
    list_local_snapshots, restore_snapshot,
};
pub use grading::{grade_answer_photo, save_answer_photo, save_uploaded_photo};
```

- [ ] **Step 2: 跑 cargo build 确认编译**

Run: `cd src-tauri && cargo build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src-tauri/src/commands/grading.rs src-tauri/src/commands/mod.rs
git commit -m "feat(tauri): add save_uploaded_photo command"
```

---

### Task 3.5：E2E（05-manual-add）

**Files:**
- Create: `e2e/05-manual-add.spec.ts`

- [ ] **Step 1: 写 E2E**

`e2e/05-manual-add.spec.ts`:

```ts
import { test, expect } from "./fixtures";

test.describe("#2 手动加题", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
    await page.evaluate(async () => {
      try { await (window as any).__TEST_CLEAR_DATA__(); } catch {}
    });
  });

  test("添加一条错题并出现在错题本", async ({ page }) => {
    // setup
    await page.click("text=添加学生");
    await page.fill('input[type="text"]', "手添测试生");
    await page.selectOption("select >> nth=0", "8");
    await page.selectOption("select >> nth=1", "2");
    await page.click('button:has-text("添加")');
    await page.click("text=手添测试生");

    // open form
    await page.click("text=错题本");
    await page.click('button:has-text("添加")');
    await expect(page.locator("text=手动添加错题")).toBeVisible();

    // fill
    await page.fill("textarea", "求 x 的值: 2x+5=13");
    await page.fill('input[placeholder="章节"]', "一元一次方程");
    await page.fill('input[placeholder="参考答案"]', "x=4");
    await page.click('button:has-text("完成添加")');

    // verify in db
    await expect(page.locator("text=求 x 的值: 2x+5=13")).toBeVisible({ timeout: 5000 });
    const count = await page.evaluate(async () => {
      const mod = await import("/src/lib/db.ts");
      const db = await mod.getDb();
      const r = await db.select("SELECT COUNT(*) as c FROM questions WHERE content = ?", ["求 x 的值: 2x+5=13"]);
      return r[0].c;
    });
    expect(count).toBe(1);
  });

  test("空内容提交显示错误", async ({ page }) => {
    await page.click("text=添加学生");
    await page.fill('input[type="text"]', "校验测试生");
    await page.selectOption("select >> nth=0", "8");
    await page.selectOption("select >> nth=1", "2");
    await page.click('button:has-text("添加")');
    await page.click("text=校验测试生");
    await page.click("text=错题本");
    await page.click('button:has-text("添加")');
    await page.click('button:has-text("完成添加")');
    await expect(page.locator("text=题目内容不能为空")).toBeVisible();
  });
});
```

- [ ] **Step 2: 跑 E2E**

Run: `npx playwright test e2e/05-manual-add.spec.ts --reporter=list`
Expected: 2 passed

- [ ] **Step 3: Commit**

```bash
git add e2e/05-manual-add.spec.ts
git commit -m "test(e2e): #2 manual add end-to-end"
```

---

## Phase 4：#19 考前模式

### Task 4.1：TDD 写 examPrep 纯函数

**Files:**
- Create: `src/lib/examPrep.ts`
- Create: `src/lib/examPrep.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/examPrep.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  buildExamPrepQuery,
  sortQuestionsByMastery,
  getLeafKnowledgeIds,
} from "./examPrep";

describe("buildExamPrepQuery", () => {
  it("uses IN clause for multiple knowledge ids", () => {
    const { sql, params } = buildExamPrepQuery(42, [1, 2, 3]);
    expect(sql).toContain("WHERE q.student_id = ?");
    expect(sql).toContain("AND qk.knowledge_id IN (?,?,?)");
    expect(sql).toContain("ORDER BY q.mastery_score ASC");
    expect(params).toEqual([42, 1, 2, 3]);
  });

  it("uses default limit of 50", () => {
    const { sql } = buildExamPrepQuery(1, [1]);
    expect(sql).toContain("LIMIT 50");
  });

  it("respects custom limit", () => {
    const { sql, params } = buildExamPrepQuery(1, [1], 10);
    expect(sql).toContain("LIMIT 10");
    expect(params).toEqual([1, 1, 10]);
  });
});

describe("sortQuestionsByMastery", () => {
  it("sorts ascending (weakest first)", () => {
    const qs = [
      { id: 1, mastery_score: 80 },
      { id: 2, mastery_score: 30 },
      { id: 3, mastery_score: 60 },
    ] as any;
    const sorted = sortQuestionsByMastery(qs, "asc");
    expect(sorted.map((q) => q.id)).toEqual([2, 3, 1]);
  });

  it("sorts descending (strongest first)", () => {
    const qs = [{ id: 1, mastery_score: 80 }, { id: 2, mastery_score: 30 }] as any;
    expect(sortQuestionsByMastery(qs, "desc").map((q) => q.id)).toEqual([1, 2]);
  });
});

describe("getLeafKnowledgeIds", () => {
  it("returns ids of nodes with no children", () => {
    const tree = [
      {
        node: { id: 1 },
        children: [
          { node: { id: 2 }, children: [] },
          { node: { id: 3 }, children: [{ node: { id: 4 }, children: [] }] },
        ],
      },
    ] as any;
    const ids = getLeafKnowledgeIds(tree);
    expect(ids.sort()).toEqual([2, 4].sort());
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest --exclude e2e --run src/lib/examPrep.test.ts`
Expected: FAIL

- [ ] **Step 3: 写最小实现**

`src/lib/examPrep.ts`:

```ts
export type QuestionLite = { id: number; mastery_score: number };
export type TreeLike = {
  node: { id: number };
  children: TreeLike[];
};

export function buildExamPrepQuery(
  studentId: number,
  knowledgeIds: number[],
  limit = 50
): { sql: string; params: unknown[] } {
  const placeholders = knowledgeIds.map(() => "?").join(",");
  return {
    sql: `
      SELECT DISTINCT q.*
      FROM questions q
      JOIN question_knowledge qk ON q.id = qk.question_id
      WHERE q.student_id = ?
        AND q.status = 'active'
        AND qk.knowledge_id IN (${placeholders})
      ORDER BY q.mastery_score ASC
      LIMIT ?
    `,
    params: [studentId, ...knowledgeIds, limit],
  };
}

export function sortQuestionsByMastery<T extends { mastery_score: number }>(
  questions: T[],
  order: "asc" | "desc"
): T[] {
  const sorted = [...questions];
  sorted.sort((a, b) =>
    order === "asc"
      ? a.mastery_score - b.mastery_score
      : b.mastery_score - a.mastery_score
  );
  return sorted;
}

export function getLeafKnowledgeIds(tree: TreeLike[]): number[] {
  const out: number[] = [];
  function walk(node: TreeLike) {
    if (node.children.length === 0) {
      out.push(node.node.id);
    } else {
      node.children.forEach(walk);
    }
  }
  tree.forEach(walk);
  return out;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest --exclude e2e --run src/lib/examPrep.test.ts`
Expected: 5 passed

- [ ] **Step 5: Commit**

```bash
git add src/lib/examPrep.ts src/lib/examPrep.test.ts
git commit -m "feat(exam-prep): pure functions for exam prep query and sort"
```

---

### Task 4.2：KnowledgeTree 增加 multiSelect 模式

**Files:**
- Modify: `src/components/graph/KnowledgeTree.tsx`

- [ ] **Step 1: 加 prop + checkbox**

读现有 `KnowledgeTree.tsx` 和 `TreeNode`，给组件 props 加 `multiSelect?: boolean` 和 `selectedIds?: Set<number>` / `onToggle?: (id: number) => void`：

```ts
interface Props {
  tree: KnowledgeTreeNode[];
  selectedId?: number;
  onSelect: (node: KnowledgeTreeNode) => void;
  multiSelect?: boolean;
  selectedIds?: Set<number>;
  onToggle?: (id: number) => void;
}
```

`TreeNode` 内部，节点左侧（在 Chevron 之前）加 checkbox（仅 `multiSelect` 为真时）：

```tsx
{multiSelect && (
  <input
    type="checkbox"
    checked={selectedIds?.has(item.node.id) ?? false}
    onChange={(e) => {
      e.stopPropagation();
      onToggle?.(item.node.id);
    }}
    onClick={(e) => e.stopPropagation()}
    className="w-3.5 h-3.5 mr-1"
  />
)}
```

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/components/graph/KnowledgeTree.tsx
git commit -m "feat(graph-tree): add multiSelect mode with checkboxes"
```

---

### Task 4.3：PracticePage 接入考前模式

**Files:**
- Modify: `src/pages/student/PracticePage.tsx`

- [ ] **Step 1: 加 segmented control + 考前布局**

在 PracticePage 顶栏加模式切换：

```ts
const [mode, setMode] = useState<"daily" | "exam">("daily");
const [examKps, setExamKps] = useState<Set<number>>(new Set());
const [examQuestions, setExamQuestions] = useState<Question[]>([]);
```

顶栏 segmented control：

```tsx
<div className="inline-flex border border-notion-border rounded-notion overflow-hidden text-sm">
  <button
    onClick={() => setMode("daily")}
    className={`px-3 py-1 ${mode === "daily" ? "bg-notion-accent-bg text-notion-text" : "text-notion-muted"}`}
  >
    日常
  </button>
  <button
    onClick={() => setMode("exam")}
    className={`px-3 py-1 ${mode === "exam" ? "bg-notion-accent-bg text-notion-text" : "text-notion-muted"}`}
  >
    考前
  </button>
</div>
```

考前模式布局（在主内容区条件渲染）：

```tsx
{mode === "exam" && currentStudent ? (
  <ExamPrepPanel
    studentId={currentStudent.id}
    subject={filterSubject === "all" ? "math" : filterSubject}
    selectedKps={examKps}
    onToggleKp={(id) => {
      setExamKps((s) => {
        const next = new Set(s);
        if (next.has(id)) next.delete(id);
        else next.add(id);
        return next;
      });
    }}
    questions={examQuestions}
    onQuestionsChange={setExamQuestions}
    studentName={currentStudent.name}
  />
) : (
  // 现有的 daily 模式 UI 保持
  <>{/* existing selection list */}</>
)}
```

新建组件 `src/components/practice/ExamPrepPanel.tsx`：

```tsx
import { useEffect, useState } from "react";
import { useKnowledgeGraph } from "../../hooks/useKnowledgeGraph";
import KnowledgeTree from "../graph/KnowledgeTree";
import { getDb } from "../../lib/db";
import { buildExamPrepQuery, type TreeLike } from "../../lib/examPrep";
import ExportButtonGroup from "../export/ExportButtonGroup";
import type { Question } from "../../types";

interface Props {
  studentId: number;
  subject: "math" | "physics";
  selectedKps: Set<number>;
  onToggleKp: (id: number) => void;
  questions: Question[];
  onQuestionsChange: (qs: Question[]) => void;
  studentName: string;
}

export default function ExamPrepPanel({
  studentId, subject, selectedKps, onToggleKp, questions, onQuestionsChange, studentName,
}: Props) {
  const { tree } = useKnowledgeGraph({ studentId, subject });
  const [loading, setLoading] = useState(false);

  useEffect(() => {
    if (selectedKps.size === 0) {
      onQuestionsChange([]);
      return;
    }
    setLoading(true);
    (async () => {
      const { sql, params } = buildExamPrepQuery(studentId, Array.from(selectedKps));
      const db = await getDb();
      const rows = await db.select<Question[]>(sql, params);
      onQuestionsChange(rows);
      setLoading(false);
    })();
  }, [studentId, Array.from(selectedKps).join(",")]);

  return (
    <div className="flex gap-4 h-full min-h-0">
      <div className="w-72 overflow-auto notion-card">
        <h3 className="text-sm font-medium mb-2">选择知识点</h3>
        <KnowledgeTree
          tree={tree}
          multiSelect
          selectedIds={selectedKps}
          onToggle={onToggleKp}
          onSelect={() => {}}
        />
      </div>
      <div className="flex-1 overflow-auto space-y-3">
        <div className="text-xs text-notion-muted">
          {loading ? "加载中…" : `已选 ${selectedKps.size} 个知识点 / 共 ${questions.length} 道错题`}
        </div>
        {questions.length > 0 && (
          <ExportButtonGroup
            questions={questions}
            studentName={studentName}
            mode="questions_only"
            title="考前复习卷"
          />
        )}
        <div className="space-y-1">
          {questions.map((q) => (
            <div key={q.id} className="notion-card text-sm">
              <span className="text-notion-muted mr-2">{q.chapter ?? "未分类"}</span>
              {q.content}
            </div>
          ))}
        </div>
      </div>
    </div>
  );
}
```

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/pages/student/PracticePage.tsx src/components/practice/ExamPrepPanel.tsx
git commit -m "feat(exam-prep): wire exam mode panel with multi-select knowledge tree"
```

---

### Task 4.4：E2E（06-exam-prep）

**Files:**
- Create: `e2e/06-exam-prep.spec.ts`

- [ ] **Step 1: 写 E2E**

`e2e/06-exam-prep.spec.ts`:

```ts
import { test, expect } from "./fixtures";

test.describe("#19 考前模式", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
    await page.evaluate(async () => {
      try { await (window as any).__TEST_CLEAR_DATA__(); } catch {}
    });
  });

  test("切到考前模式，选择知识点后显示错题", async ({ page }) => {
    // setup student + questions
    await page.click("text=添加学生");
    await page.fill('input[type="text"]', "考前测试生");
    await page.selectOption("select >> nth=0", "8");
    await page.selectOption("select >> nth=1", "2");
    await page.click('button:has-text("添加")');
    await page.click("text=考前测试生");

    // seed
    await page.evaluate(async () => {
      const mod = await import("/src/lib/db.ts");
      const db = await mod.getDb();
      const s = await db.select("SELECT id FROM students WHERE name = ?", ["考前测试生"]);
      const sid = s[0].id;
      for (const c of ["分式", "一元一次方程"]) {
        const r = await db.execute(
          `INSERT INTO knowledge_nodes (id, subject, grade, semester, chapter, name, parent_id, is_preset)
           VALUES ((SELECT COALESCE(MAX(id),0)+1 FROM knowledge_nodes), 'math', 8, 2, ?, ?, NULL, 1)`,
          [c, c]
        );
        const kpId = r.lastInsertId;
        const qr = await db.execute(
          `INSERT INTO questions (student_id, subject, source_type, question_type, content,
            correct_answer, mastery_score, chapter, status)
           VALUES (?, 'math', 'manual', 'objective', ?, 'A', 30, ?, 'active')`,
          [sid, `题-${c}`, c]
        );
        await db.execute(
          "INSERT INTO question_knowledge (question_id, knowledge_id, confidence) VALUES (?, ?, 1.0)",
          [qr.lastInsertId, kpId]
        );
      }
    });

    // navigate
    await page.click("text=练习卷");
    await page.click('button:has-text("考前")');
    await expect(page.locator("text=选择知识点")).toBeVisible();

    // expand tree and select
    const checkboxes = page.locator('input[type="checkbox"]');
    const count = await checkboxes.count();
    if (count > 0) {
      await checkboxes.first().check();
    }

    // wait for query
    await page.waitForTimeout(500);
    // should show at least one question
    await expect(page.locator("text=题-")).toBeVisible({ timeout: 5000 });
  });
});
```

- [ ] **Step 2: 跑 E2E**

Run: `npx playwright test e2e/06-exam-prep.spec.ts --reporter=list`
Expected: 1 passed

- [ ] **Step 3: Commit**

```bash
git add e2e/06-exam-prep.spec.ts
git commit -m "test(e2e): #19 exam prep end-to-end"
```

---

## Phase 5：M11 备份

### Task 5.1：TDD 写 backupConfig 纯函数

**Files:**
- Create: `src/lib/backupConfig.ts`
- Create: `src/lib/backupConfig.test.ts`

- [ ] **Step 1: 写失败测试**

`src/lib/backupConfig.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { formatRelativeTime, formatBytes, sortSnapshotsByTime } from "./backupConfig";

describe("formatRelativeTime", () => {
  const now = new Date("2026-06-02T10:00:00Z");
  it("returns 刚刚 for < 60s", () => {
    expect(formatRelativeTime("2026-06-02T09:59:30Z", now)).toBe("刚刚");
  });
  it("returns N 分钟前", () => {
    expect(formatRelativeTime("2026-06-02T09:55:00Z", now)).toBe("5 分钟前");
  });
  it("returns N 小时前", () => {
    expect(formatRelativeTime("2026-06-02T07:00:00Z", now)).toBe("3 小时前");
  });
  it("returns 昨天 for yesterday", () => {
    expect(formatRelativeTime("2026-06-01T10:00:00Z", now)).toBe("昨天");
  });
  it("returns N 天前", () => {
    expect(formatRelativeTime("2026-05-28T10:00:00Z", now)).toBe("5 天前");
  });
});

describe("formatBytes", () => {
  it("formats bytes", () => { expect(formatBytes(500)).toBe("500 B"); });
  it("formats KB", () => { expect(formatBytes(2048)).toBe("2.0 KB"); });
  it("formats MB", () => { expect(formatBytes(5 * 1024 * 1024)).toBe("5.0 MB"); });
  it("formats GB", () => { expect(formatBytes(2 * 1024 * 1024 * 1024)).toBe("2.0 GB"); });
});

describe("sortSnapshotsByTime", () => {
  it("sorts by created_at desc", () => {
    const s = [
      { path: "a", created_at: "2026-01-01", size_bytes: 1 },
      { path: "b", created_at: "2026-06-01", size_bytes: 1 },
    ];
    expect(sortSnapshotsByTime(s).map(x => x.path)).toEqual(["b", "a"]);
  });
});
```

- [ ] **Step 2: 跑测试确认失败**

Run: `npx vitest --exclude e2e --run src/lib/backupConfig.test.ts`
Expected: FAIL

- [ ] **Step 3: 写实现**

`src/lib/backupConfig.ts`:

```ts
export type SnapshotInfo = {
  path: string;
  created_at: string;
  size_bytes: number;
};

export function formatRelativeTime(ts: string, now: Date = new Date()): string {
  const t = new Date(ts).getTime();
  const diffSec = Math.floor((now.getTime() - t) / 1000);
  if (diffSec < 60) return "刚刚";
  if (diffSec < 3600) return `${Math.floor(diffSec / 60)} 分钟前`;
  if (diffSec < 86400) return `${Math.floor(diffSec / 3600)} 小时前`;
  const days = Math.floor(diffSec / 86400);
  if (days === 1) return "昨天";
  return `${days} 天前`;
}

export function formatBytes(bytes: number): string {
  if (bytes < 1024) return `${bytes} B`;
  if (bytes < 1024 * 1024) return `${(bytes / 1024).toFixed(1)} KB`;
  if (bytes < 1024 * 1024 * 1024) return `${(bytes / (1024 * 1024)).toFixed(1)} MB`;
  return `${(bytes / (1024 * 1024 * 1024)).toFixed(1)} GB`;
}

export function sortSnapshotsByTime(snapshots: SnapshotInfo[]): SnapshotInfo[] {
  return [...snapshots].sort((a, b) =>
    a.created_at < b.created_at ? 1 : a.created_at > b.created_at ? -1 : 0
  );
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `npx vitest --exclude e2e --run src/lib/backupConfig.test.ts`
Expected: 10 passed

- [ ] **Step 5: Commit**

```bash
git add src/lib/backupConfig.ts src/lib/backupConfig.test.ts
git commit -m "feat(backup): pure functions for backup metadata"
```

---

### Task 5.2：添加 app_config 表 migration

**Files:**
- Create: `src-tauri/src/db/migrations/v4_add_app_config.sql`
- Modify: `src-tauri/src/db/migrations.rs`
- Modify: `public/db-schema.sql`

- [ ] **Step 1: 写 migration**

`src-tauri/src/db/migrations/v4_add_app_config.sql`:

```sql
CREATE TABLE IF NOT EXISTS app_config (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

`src-tauri/src/db/migrations.rs` 在 `MIGRATIONS` 数组或加载函数（按现有实现）的末尾追加 v4 引用，文件名 `v4_add_app_config.sql`。具体语法按当前 `migrations.rs` 的风格保持一致。

`public/db-schema.sql` 在合适位置追加：

```sql
CREATE TABLE IF NOT EXISTS app_config (
  key TEXT PRIMARY KEY,
  value TEXT,
  updated_at TEXT NOT NULL DEFAULT (datetime('now'))
);
```

- [ ] **Step 2: 跑 cargo build 验证**

Run: `cd src-tauri && cargo build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src-tauri/src/db/migrations/v4_add_app_config.sql src-tauri/src/db/migrations.rs public/db-schema.sql
git commit -m "feat(db): add app_config table for backup settings"
```

---

### Task 5.3：Rust 端 backup 命令

**Files:**
- Create: `src-tauri/src/commands/backup.rs`
- Modify: `src-tauri/src/commands/mod.rs`
- Modify: `src-tauri/src/lib.rs`

- [ ] **Step 1: 写 Rust 模块**

`src-tauri/src/commands/backup.rs`:

```rust
use serde::Serialize;
use std::fs;
use std::path::PathBuf;
use std::time::SystemTime;
use tauri::{AppHandle, Manager};

#[derive(Serialize)]
pub struct SnapshotInfo {
    pub path: String,
    pub created_at: String,
    pub size_bytes: u64,
}

fn snapshots_dir(app: &AppHandle) -> Result<PathBuf, String> {
    let dir = app
        .path()
        .app_data_dir()
        .map_err(|e| e.to_string())?
        .join("backups");
    fs::create_dir_all(&dir).map_err(|e| e.to_string())?;
    Ok(dir)
}

fn timestamp() -> String {
    let now = chrono::Utc::now();
    now.format("%Y%m%d-%H%M%S").to_string()
}

fn snapshot_for_db(db_path: &PathBuf, suffix: &str) -> Result<SnapshotInfo, String> {
    let file_name = format!("snapshot-{}.db", suffix);
    let dest = db_path.parent().unwrap().join("backups").join(&file_name);
    fs::copy(db_path, &dest).map_err(|e| e.to_string())?;
    let meta = fs::metadata(&dest).map_err(|e| e.to_string())?;
    Ok(SnapshotInfo {
        path: dest.to_string_lossy().to_string(),
        created_at: chrono::Utc::now().to_rfc3339(),
        size_bytes: meta.len(),
    })
}

fn db_path(app: &AppHandle) -> Result<PathBuf, String> {
    let dir = app.path().app_data_dir().map_err(|e| e.to_string())?;
    Ok(dir.join("seeker.db"))
}

#[tauri::command]
pub fn create_local_snapshot(app: AppHandle) -> Result<SnapshotInfo, String> {
    let dir = snapshots_dir(&app)?;
    fs::create_dir_all(&dir).map_err(|e| e.to_string())?;
    let db = db_path(&app)?;
    let info = snapshot_for_db(&db, &timestamp())?;
    cleanup_old_snapshots_inner(&dir, 10)?;
    Ok(info)
}

fn cleanup_old_snapshots_inner(dir: &PathBuf, keep: usize) -> Result<usize, String> {
    let mut entries: Vec<_> = fs::read_dir(dir)
        .map_err(|e| e.to_string())?
        .filter_map(|e| e.ok())
        .filter(|e| e.file_name().to_string_lossy().starts_with("snapshot-"))
        .collect();
    entries.sort_by_key(|e| e.metadata().and_then(|m| m.modified()).ok());
    let mut deleted = 0;
    if entries.len() > keep {
        for e in &entries[..entries.len() - keep] {
            if fs::remove_file(e.path()).is_ok() {
                deleted += 1;
            }
        }
    }
    Ok(deleted)
}

#[tauri::command]
pub fn cleanup_old_snapshots(app: AppHandle, keep: usize) -> Result<usize, String> {
    let dir = snapshots_dir(&app)?;
    cleanup_old_snapshots_inner(&dir, keep)
}

#[tauri::command]
pub fn list_local_snapshots(app: AppHandle) -> Result<Vec<SnapshotInfo>, String> {
    let dir = snapshots_dir(&app)?;
    let mut out: Vec<SnapshotInfo> = Vec::new();
    for e in fs::read_dir(&dir).map_err(|e| e.to_string())?.filter_map(|e| e.ok()) {
        let name = e.file_name().to_string_lossy().to_string();
        if !name.starts_with("snapshot-") { continue; }
        let meta = e.metadata().map_err(|e| e.to_string())?;
        let modified = meta.modified().unwrap_or(SystemTime::now());
        let dt: chrono::DateTime<chrono::Utc> = modified.into();
        out.push(SnapshotInfo {
            path: e.path().to_string_lossy().to_string(),
            created_at: dt.to_rfc3339(),
            size_bytes: meta.len(),
        });
    }
    out.sort_by(|a, b| a.created_at.cmp(&b.created_at).reverse());
    Ok(out)
}

#[tauri::command]
pub fn restore_snapshot(app: AppHandle, path: String) -> Result<(), String> {
    // safety net: snapshot current db first
    let _ = create_local_snapshot(app.clone());
    let db = db_path(&app)?;
    fs::copy(&path, &db).map_err(|e| e.to_string())?;
    Ok(())
}

#[tauri::command]
pub fn backup_to_sync_folder(app: AppHandle, sync_folder: String) -> Result<SnapshotInfo, String> {
    fs::create_dir_all(&sync_folder).map_err(|e| e.to_string())?;
    let db = db_path(&app)?;
    let dest = PathBuf::from(&sync_folder).join(format!("seeker-{}.db", timestamp()));
    fs::copy(&db, &dest).map_err(|e| e.to_string())?;
    let meta = fs::metadata(&dest).map_err(|e| e.to_string())?;
    Ok(SnapshotInfo {
        path: dest.to_string_lossy().to_string(),
        created_at: chrono::Utc::now().to_rfc3339(),
        size_bytes: meta.len(),
    })
}
```

- [ ] **Step 2: 在 mod.rs 暴露**

`src-tauri/src/commands/mod.rs` 加 `pub mod backup;` 和 `pub use backup::*;`

- [ ] **Step 3: 在 lib.rs 注册 + 退出时 hook**

`src-tauri/src/lib.rs`（`run` 函数）注册命令：

```rust
.invoke_handler(tauri::generate_handler![
    commands::create_local_snapshot,
    commands::list_local_snapshots,
    commands::restore_snapshot,
    commands::cleanup_old_snapshots,
    commands::backup_to_sync_folder,
    // ... 其它已有
])
```

加退出时 hook（在 `setup` 或 main 中）：

```rust
.on_window_event(|window, event| {
    if let tauri::WindowEvent::CloseRequested { .. } = event {
        let _ = commands::create_local_snapshot(window.app_handle().clone());
    }
})
```

（语法按现有 `tauri::Builder` 链式调用结构追加）

- [ ] **Step 4: 跑 cargo build 验证**

Run: `cd src-tauri && cargo build`
Expected: 通过

- [ ] **Step 5: Commit**

```bash
git add src-tauri/src/commands/backup.rs src-tauri/src/commands/mod.rs src-tauri/src/lib.rs
git commit -m "feat(backup): Rust commands for snapshot, list, restore, cleanup, sync-folder backup"
```

---

### Task 5.4：TS 包装

**Files:**
- Create: `src/lib/backup.ts`

- [ ] **Step 1: 写 wrapper**

`src/lib/backup.ts`:

```ts
import { invoke } from "@tauri-apps/api/core";
import { open } from "@tauri-apps/plugin-dialog";
import type { SnapshotInfo } from "./backupConfig";
export type { SnapshotInfo } from "./backupConfig";

export async function pickSyncFolder(): Promise<string | null> {
  return (await open({ directory: true, multiple: false })) as string | null;
}

export async function createLocalSnapshot(): Promise<SnapshotInfo> {
  return invoke("create_local_snapshot");
}

export async function listLocalSnapshots(): Promise<SnapshotInfo[]> {
  return invoke("list_local_snapshots");
}

export async function restoreSnapshot(path: string): Promise<void> {
  return invoke("restore_snapshot", { path });
}

export async function backupToSyncFolder(syncFolder: string): Promise<SnapshotInfo> {
  return invoke("backup_to_sync_folder", { syncFolder });
}

export async function getSyncFolderConfig(): Promise<string | null> {
  const db = await (await import("./db")).getDb();
  const r = await db.select<{ value: string }[]>(
    "SELECT value FROM app_config WHERE key = ?",
    ["sync_folder"]
  );
  return r[0]?.value ?? null;
}

export async function setSyncFolderConfig(path: string | null): Promise<void> {
  const db = await (await import("./db")).getDb();
  if (path) {
    await db.execute(
      "INSERT OR REPLACE INTO app_config (key, value, updated_at) VALUES (?, ?, datetime('now'))",
      ["sync_folder", path]
    );
  } else {
    await db.execute("DELETE FROM app_config WHERE key = ?", ["sync_folder"]);
  }
}
```

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/lib/backup.ts
git commit -m "feat(backup): TS wrapper for Rust commands + config CRUD"
```

---

### Task 5.5：重写 SettingsPage

**Files:**
- Modify: `src/pages/parent/SettingsPage.tsx`

- [ ] **Step 1: 写新 SettingsPage**

`src/pages/parent/SettingsPage.tsx` 完整替换：

```tsx
import { useEffect, useState } from "react";
import { Folder, Plus, RefreshCw, Trash2, FolderOpen, Download } from "lucide-react";
import {
  pickSyncFolder,
  createLocalSnapshot,
  listLocalSnapshots,
  restoreSnapshot,
  backupToSyncFolder,
  getSyncFolderConfig,
  setSyncFolderConfig,
  type SnapshotInfo,
} from "../../lib/backup";
import { formatRelativeTime, formatBytes } from "../../lib/backupConfig";

export default function SettingsPage() {
  const [syncFolder, setSyncFolder] = useState<string | null>(null);
  const [snapshots, setSnapshots] = useState<SnapshotInfo[]>([]);
  const [pendingRestore, setPendingRestore] = useState<string | null>(null);
  const [busy, setBusy] = useState(false);
  const [toast, setToast] = useState<string | null>(null);

  async function refresh() {
    const [cfg, snaps] = await Promise.all([
      getSyncFolderConfig(),
      listLocalSnapshots().catch(() => [] as SnapshotInfo[]),
    ]);
    setSyncFolder(cfg);
    setSnapshots(snaps);
  }
  useEffect(() => { refresh(); }, []);

  function flash(msg: string) {
    setToast(msg);
    setTimeout(() => setToast(null), 3000);
  }

  async function handlePickFolder() {
    const p = await pickSyncFolder();
    if (p) {
      await setSyncFolderConfig(p);
      setSyncFolder(p);
      flash("已保存");
    }
  }

  async function handleCreateSnapshot() {
    setBusy(true);
    try {
      await createLocalSnapshot();
      await refresh();
      flash("快照已创建");
    } finally { setBusy(false); }
  }

  async function handleBackupToSync() {
    if (!syncFolder) return;
    setBusy(true);
    try {
      await backupToSyncFolder(syncFolder);
      flash("已备份到同步盘");
    } finally { setBusy(false); }
  }

  async function handleRestore(path: string) {
    if (pendingRestore === path) {
      setBusy(true);
      try {
        await restoreSnapshot(path);
        flash("已恢复，应用将重启");
      } finally {
        setBusy(false);
        setPendingRestore(null);
      }
    } else {
      setPendingRestore(path);
    }
  }

  return (
    <div className="space-y-6 max-w-2xl">
      <h2 className="text-xl font-semibold">设置</h2>

      <section className="notion-card space-y-3">
        <h3 className="text-sm font-medium">同步盘备份</h3>
        <div className="text-sm">
          {syncFolder ? (
            <code className="text-xs text-notion-muted break-all">{syncFolder}</code>
          ) : (
            <span className="text-notion-muted">未设置</span>
          )}
        </div>
        <div className="flex gap-2">
          <button onClick={handlePickFolder} className="notion-btn-ghost text-sm">
            <Folder size={14} /> {syncFolder ? "更换" : "选择文件夹"}
          </button>
          {syncFolder && (
            <button onClick={handleBackupToSync} disabled={busy} className="notion-btn-primary text-sm">
              <Download size={14} /> 立即备份
            </button>
          )}
        </div>
      </section>

      <section className="notion-card space-y-3">
        <div className="flex items-center justify-between">
          <h3 className="text-sm font-medium">本地快照</h3>
          <button onClick={handleCreateSnapshot} disabled={busy} className="notion-btn-ghost text-sm">
            <Plus size={14} /> 立即创建快照
          </button>
        </div>
        {snapshots.length === 0 ? (
          <p className="text-sm text-notion-muted">暂无快照</p>
        ) : (
          <ul className="divide-y divide-notion-border">
            {snapshots.map((s) => (
              <li key={s.path} className="py-2 flex items-center gap-3 text-sm">
                <span className="flex-1">{formatRelativeTime(s.created_at)}</span>
                <span className="text-notion-muted">{formatBytes(s.size_bytes)}</span>
                {pendingRestore === s.path ? (
                  <>
                    <span className="text-xs text-notion-danger">确认恢复？</span>
                    <button onClick={() => handleRestore(s.path)} disabled={busy} className="notion-btn-danger text-xs">
                      确认
                    </button>
                    <button onClick={() => setPendingRestore(null)} className="notion-btn-ghost text-xs">取消</button>
                  </>
                ) : (
                  <button onClick={() => handleRestore(s.path)} className="notion-btn-ghost text-xs">
                    <RefreshCw size={12} /> 恢复
                  </button>
                )}
              </li>
            ))}
          </ul>
        )}
      </section>

      {toast && (
        <div className="fixed top-4 right-4 notion-card text-sm shadow-notion-modal">
          {toast}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 2: 跑 build 验证**

Run: `npm run build`
Expected: 通过

- [ ] **Step 3: Commit**

```bash
git add src/pages/parent/SettingsPage.tsx
git commit -m "feat(backup): SettingsPage with sync folder, snapshots, restore inline confirm"
```

---

### Task 5.6：E2E（07-backup）

**Files:**
- Create: `e2e/07-backup.spec.ts`

- [ ] **Step 1: 写 E2E**

`e2e/07-backup.spec.ts`:

```ts
import { test, expect } from "./fixtures";

test.describe("M11 备份", () => {
  test.beforeEach(async ({ page }) => {
    await page.goto("/");
    await page.evaluate(async () => {
      try { await (window as any).__TEST_CLEAR_DATA__(); } catch {}
    });
  });

  test("设置页可切换到本地快照 tab（mock 命令）", async ({ page }) => {
    // add student
    await page.click("text=添加学生");
    await page.fill('input[type="text"]', "备份测试生");
    await page.selectOption("select >> nth=0", "8");
    await page.selectOption("select >> nth=1", "2");
    await page.click('button:has-text("添加")');
    await page.click("text=备份测试生");

    // switch to parent
    await page.getByRole("button", { name: /学生模式/ }).click();
    await page.click("text=设置");
    await expect(page.locator("h2:has-text('设置')")).toBeVisible();
    await expect(page.locator("text=同步盘备份")).toBeVisible();
    await expect(page.locator("text=本地快照")).toBeVisible();
  });

  test("snapshot 元数据格式化正确（纯函数）", async ({ page }) => {
    const out = await page.evaluate(async () => {
      const mod = await import("/src/lib/backupConfig.ts");
      const now = new Date("2026-06-02T10:00:00Z");
      return {
        just: mod.formatRelativeTime("2026-06-02T09:59:30Z", now),
        minutes: mod.formatRelativeTime("2026-06-02T09:55:00Z", now),
        hours: mod.formatRelativeTime("2026-06-02T07:00:00Z", now),
        yesterday: mod.formatRelativeTime("2026-06-01T10:00:00Z", now),
        days: mod.formatRelativeTime("2026-05-28T10:00:00Z", now),
        mb: mod.formatBytes(5 * 1024 * 1024),
      };
    });
    expect(out.just).toBe("刚刚");
    expect(out.minutes).toBe("5 分钟前");
    expect(out.hours).toBe("3 小时前");
    expect(out.yesterday).toBe("昨天");
    expect(out.days).toBe("5 天前");
    expect(out.mb).toBe("5.0 MB");
  });
});
```

- [ ] **Step 2: 跑 E2E**

Run: `npx playwright test e2e/07-backup.spec.ts --reporter=list`
Expected: 2 passed

- [ ] **Step 3: Commit**

```bash
git add e2e/07-backup.spec.ts
git commit -m "test(e2e): M11 backup end-to-end (settings + pure function sanity)"
```

---

## Phase 6：集成测试 + 全 E2E 回归

### Task 6.1：跑全单元测试

- [ ] **Step 1: 跑**

Run: `npx vitest --exclude e2e --run`
Expected: 全部 passed（应有 95 + 7 + 5 + 5 + 10 = 122 个左右测试通过）

- [ ] **Step 2: 跑 build**

Run: `npm run build`
Expected: 通过

---

### Task 6.2：跑全部 Playwright E2E

- [ ] **Step 1: 跑**

Run: `npx playwright test --reporter=list`
Expected: 全部 spec.ts 通过

如果现有 spec 因为 UI 改动失效，按 text 断言稳定优先原则微调。

---

### Task 6.3：最终 commit + push

- [ ] **Step 1: 确认 git status 干净**

Run: `git status`
Expected: working tree clean

- [ ] **Step 2: push**

Run: `git push origin main`
Expected: pushed successfully

---

## 验收清单

- [ ] Phase 1-5 全部 task 走完，每个 task 都有对应 commit
- [ ] 单元测试全绿（约 122 个）
- [ ] 4 个新 E2E spec（04-07）+ 1 个 style smoke（08）全过
- [ ] 现有 95 单元 + 6 E2E 不退化
- [ ] `npm run build` 通过
- [ ] push 到 origin/main
